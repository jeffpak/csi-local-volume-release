#!/bin/bash

set -e -x

DATA_DIR=<%= p("csi_localdriver.plugin_path") %>
mkdir -p $DATA_DIR
chown vcap:vcap $DATA_DIR

MOUNT_DIR=<%= p("csi_localdriver.volumes_root") %>
mkdir -p $MOUNT_DIR
chown vcap:vcap $MOUNT_DIR

echo "Installed csi-localdriver paths"
exit 0
